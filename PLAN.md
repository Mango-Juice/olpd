# 「죽을 때마다 한 줄」 첫 제작 계획 — UI와 플레이 경험 우선

## 1. 목표와 제작 원칙

**귀여운 용사에게 한 줄씩 지침을 남겨 던전을 탈출시키는 웹 게임을 만든다. 가장 중요한 성공 기준은 완성도 높은 UI와 부드러운 플레이 경험이다.**

플레이어는 지침을 작성하고, 용사가 그 말을 따라 움직이는 모습을 지켜본다. 실패하면 지침을 추가하거나 과거의 지침을 삭제한다. 마지막에는 자신이 가르친 용사가 연속으로 장애물을 통과하는 만족감을 제공한다.

### 우선순위

1. **화면의 매력과 일관성:** 캐릭터, 던전, 메모장, 입력창이 하나의 게임처럼 보인다.
2. **부드러운 상호작용:** 입력, 이동, 사망, 부활, 재도전이 자연스럽게 연결된다.
3. **납득할 수 있는 행동:** 내 문장이 어떤 행동으로 이어졌는지 이해할 수 있다.
4. **안정적인 실제 기능:** AI 연결, 저장, 점수, 배포가 정확히 작동한다.
5. **콘텐츠 분량:** 위 품질을 확보한 다음 방을 확장한다.

테스트 통과와 API 연결은 필수지만, 그것만으로 완성으로 판단하지 않는다. **일정이나 구현 복잡도가 문제가 되면 콘텐츠를 줄이고 UI·플레이 품질을 지킨다.** 비밀값 보호, 점수 정확성, 실제 AI 사용 원칙은 유지한다.

### 첫 버전 범위

| 항목 | 결정 |
|---|---|
| 기술 | React + TypeScript + Vite + Canvas 2D |
| 게임 시점 | 횡방향으로 보는 방 단위 던전 |
| 콘텐츠 | 튜토리얼 1개 + 본편 8개 방 목표 |
| 플레이 시간 | 첫 클리어 5~10분 목표 |
| 행동 | 전진·점프·숙이기·우회 |
| 자연어 해석 | 실제 Jev API |
| 저장 | 현재 브라우저 자동 저장 |
| 공유 | 게임 링크와 결과 요약 |
| 지원 화면 | 데스크톱과 모바일 세로 |
| 배포 | Vercel 프런트엔드 + Node.js 서버 API |

Sites는 사용하지 않는다. Google OAuth·서버 동기화·랭킹·친구 플레이 이어받기·랜덤 던전·전투·장비는 후속 범위로 둔다.

## 2. 시각 디자인과 플레이 경험

### 아트 방향

**도트의 귀여움은 살리고, 지나치게 투박한 저해상도 느낌은 피한다.**

- 캐릭터는 눈과 표정, 자세 차이가 읽히는 충분한 픽셀 밀도로 제작한다. 48~64px 크기의 원본 캐릭터를 출발점으로 삼는다.
- 짙은 남색·보랏빛 석조 던전과 따뜻한 횃불, 밝은 용사, 종이 메모장을 조합한다.
- 배경은 은은한 깊이와 움직임을 주되, 위험물과 이동 경로가 가장 잘 보이게 한다.
- 과도한 검은 외곽선, 큰 사각형 덩어리, 서로 다른 픽셀 밀도의 에셋 혼합을 피한다.
- 도트 표현은 캐릭터·환경·소품 중심으로 적용한다. 설명·입력창·버튼은 선명한 HTML UI로 만든다.
- 본문과 입력 글자는 기본 16px 이상으로 두고, 한국어에 읽기 어려운 도트 폰트를 강제하지 않는다.
- 평범한 웹 대시보드처럼 카드와 지표를 나열하지 않는다. 첫 화면부터 게임 장면과 시작 동작이 중심이다.

### 에셋 제작

- 기존 무료 에셋은 후보로만 검토한다. 편의성이나 무료 여부 때문에 최종 디자인을 맞추지 않는다.
- **필요하면 ImageGen 스킬을 사용해 스타일 시안, 캐릭터, 배경, 소품을 제작·수정한다.**
- 먼저 대표 방과 용사, 메모장이 함께 보이는 시각 기준을 정한다. 이후 모든 자산에 같은 시점·팔레트·광원·픽셀 밀도를 적용한다.
- 생성 이미지의 프레임 정렬, 캐릭터 비율, 투명 배경, 경계, 타일 반복을 검사한다. 생성된 스프라이트를 검수 없이 애니메이션에 사용하지 않는다.
- 텍스트와 버튼까지 포함한 이미지를 게임 화면으로 붙이지 않는다. 실제 조작 요소는 React로 구현한다.
- 최종 자산은 프로젝트에 포함하고 출처·라이선스 또는 생성 이력을 남긴다.

### 캐릭터와 모션

대기·걷기·점프·숙이기·우회·사망·부활·기쁨을 서로 구분한다.

- 대기 중에는 작은 호흡과 시선 움직임을 준다.
- 점프에는 도약 준비와 착지 반응을 넣어 무게감을 만든다.
- 사망은 짧고 귀엽게 표현한다. 긴 연출로 재도전을 막지 않는다.
- 부활할 때 메모장이 남아 있다는 사실을 시각적으로 강조한다.
- 지침이 적용되면 해당 줄을 잠깐 강조한다. 매번 설명창을 띄우지는 않는다.
- 삭제 시 지우개 흔적 또는 문장이 흐려지는 연출을 사용한다.
- 화면 흔들림·섬광은 절제하고 모션 감소 설정을 지원한다.
- 효과음은 첫 사용자 상호작용 이후 재생하며 음소거 설정을 저장한다.

### 화면 구성과 입력 흐름

| 상태 | 주요 화면과 행동 |
|---|---|
| 시작 | 던전과 용사를 바로 보여주고 시작·이어하기 제공 |
| 진행 | 게임 화면 중심, 현재 적용 지침을 짧게 강조 |
| 사망 | 사고 장면을 유지하며 원인·행동·적용 지침 표시 |
| 정비 | 지침 추가, 과거 지침 삭제, 재출발 |
| 해석 대기 | 입력 내용을 보존하고 용사의 대기 동작과 짧은 상태 안내 |
| 해석 확인 | “이렇게 기억할게”와 해석 내용, 확정 버튼 |
| 클리어 | 용사의 성공 연출, 기록, 최종 메모장, 공유·재도전 |

- 데스크톱은 게임 화면과 메모장을 나란히 배치한다.
- 모바일은 게임 장면·현재 상황·입력창을 우선 배치하고 긴 지침 목록은 접을 수 있게 한다.
- 키보드가 올라와도 사고 장면과 입력을 함께 확인할 수 있게 한다.
- 버튼은 즉시 눌림 상태를 보여준다. AI 응답 대기 때문에 화면 전체를 비우거나 로딩 화면으로 바꾸지 않는다.
- 한국어 조합 입력 중 Enter가 제출되지 않게 한다.
- 이미 본 동일한 행동은 3배속으로 재생한다. 행동이 달라진 구간과 처음 보는 방은 정상 속도로 보여준다.
- 탭이 숨겨지면 재생을 멈추고, 일시정지·다시 재생을 지원한다.
- 주요 이동·전환은 60fps를 목표로 실제 기기에서 확인한다. 프레임 저하 시 장식 효과를 줄여 입력과 캐릭터 움직임을 우선한다.

## 3. 게임 규칙과 콘텐츠

### 지침 규칙

- 기본 행동은 전진이다.
- 현재 상황에 적용되는 지침 중 **가장 최근에 작성한 지침**을 실행한다.
- 지침은 최대 80자로, 한 종류의 행동을 지시한다.
- “구덩이나 바닥 가시가 있으면 뛰어”처럼 여러 조건에 같은 행동을 연결하는 것은 허용한다.
- “구덩이는 뛰고 천장 가시에서는 숙여”처럼 여러 행동을 담으면 나누어 쓰도록 안내한다.
- 확정 전에는 자유롭게 편집할 수 있다. 확정 후에는 **수정·순서 변경 없이 삭제만 가능**하다.
- 본편 사망 한 번당 새 지침을 최대 한 줄 추가할 수 있다. 추가하지 않고 재도전해도 된다.
- 작성 기회는 다음 출발 때 소멸하고 누적되지 않는다.
- 사망 후 같은 던전 입구로 돌아가며, 지침·지우개·기록만 유지한다.

### 삭제와 데스

| 조건 | 처리 |
|---|---|
| 본편 시작 | 지우개 2개 지급 |
| 지우개가 있음 | 선택한 지침 한 줄 삭제, 지우개 1개 소모 |
| 지우개가 없음 | 선택한 지침 한 줄 삭제, 3데스 추가 |
| 삭제 취소 | 지침과 비용 모두 변경 없음 |
| 패널티 데스 발생 | 새 지침 작성 기회 없음 |
| 새 도전 시작 | 지침·점수 초기화, 지우개 2개 재지급 |

- 삭제는 사망 후 정비 화면에서 가능하다.
- 지우개가 있으면 먼저 사용한다. 지우개를 아끼기 위해 데스를 선택하는 기능은 넣지 않는다.
- 여러 줄을 삭제하면 각 줄에 비용을 부과한다.
- 확정 전에 대상 문장과 비용을 보여주며, 확정 후 되돌리기는 제공하지 않는다.
- 삭제와 비용 반영은 하나의 상태 변경으로 처리한다.
- 점수는 `사망·자진 부활 + 삭제 패널티`로 계산한다.
- 지우개가 없는 삭제는 “여러 번 부활한 끝에 기억을 잃었다”는 짧은 연출로 처리한다.
- 진행 불가능한 행동은 `막힘` 상태로 처리하고, “포기하고 부활하기 · +1데스”를 제공한다.

지우개 2개와 삭제 비용 3데스는 초기값이며 코드의 설정값으로 분리한다.

### 튜토리얼

1. 입구에서 기다리는 용사에게 “앞으로 전진해”를 입력한다. 같은 뜻의 자연스러운 표현도 받아들인다.
2. 해석을 확인하고 확정하면 용사가 걸어 문을 통과한다.
3. 구덩이에서 떨어지고, 사고 원인과 적용된 지침을 보여준다.
4. “구덩이가 있으면 뛰어”라는 새 지침을 작성한다.
5. 부활 후 같은 구덩이와 다른 모습의 끊어진 다리를 통과한다.
6. 별도의 연습 메모에서 삭제·지우개·소진 후 비용을 설명한다.
7. 실제로 작성한 전진·점프 지침을 본편으로 가져간다. 본편 기록은 0데스, 지우개는 2개로 시작한다.

튜토리얼의 사망과 삭제는 본편에 비용을 남기지 않는다. 전진 지침을 삭제하더라도 기본 전진은 유지되며 이를 도움말에 명시한다.

### 본편 구성

| 방 | 장애물 | 경험 |
|---|---|---|
| 1 | 구덩이 | 튜토리얼 지침 재사용 |
| 2 | 끊어진 다리 | 다른 모습에 같은 지침 적용 |
| 3 | 바닥 가시 | 점프 지침 확장 |
| 4 | 낮은 가시 통로 | 숙이기 학습 |
| 5 | 구덩이·천장 가시·샛길 | 예외와 우회 학습 |
| 6 | 바닥 가시·천장 가시·샛길 | 우회 지침 재사용 |
| 7 | 구덩이 다음 낮은 통로 | 서로 다른 지침을 연속 적용 |
| 8 | 기존 요소의 조합과 출구 | 가르친 내용으로 연속 통과 |

- 방 하나에 여러 판단 지점이 있을 수 있다.
- 우회는 표시된 샛길을 지나는 정해진 행동이다. 자유 경로 탐색은 만들지 않는다.
- 장면에서 볼 수 있는 정보와 AI가 받는 관찰 정보를 일치시킨다.
- 숨겨진 즉사 함정, 무작위 실패, 타이밍 운 요소를 넣지 않는다.
- 마지막 방에는 새로운 장애물을 추가하지 않는다.

## 4. 구현 구조, AI, 저장과 배포

### 코드 구조

| 계층 | 역할 |
|---|---|
| React UI | 입력, 메모장, 화면 상태, 확인창, 결과 |
| Canvas 렌더러 | 캐릭터·방·애니메이션·이펙트 |
| TypeScript 게임 코어 | 지침 우선순위, 행동 결과, 사망, 삭제, 점수 |
| 저장 모듈 | 자동 저장, 복원, 버전·손상 검사 |
| 서버 API | 입력 검증, Jev 호출, 해석 응답 검증 |

React와 Canvas의 갱신 주기를 분리한다. 게임 코어가 결과를 결정하고 렌더러가 재생한다. 생사 판정을 프레임 속도나 애니메이션 완료 콜백에 의존시키지 않는다.

핵심 데이터는 `Instruction`, `Observation`, `Interpretation`, `RunState`, `ExecutionEvent`, `SaveData`로 나눈다. 실행 이벤트에는 상황·행동·적용 지침·결과를 기록한다.

### 실제 AI 연결

**구현 시작 시 .env를 확인하여 사용자에게 Jev API 키 발급·설정을 명확히 요구한다.**

- 키 준비 전에는 화면과 순수 게임 로직을 개발할 수 있다.
- **모킹, 키워드 판정, 다른 모델로 실제 AI 플레이를 임의 대체하지 않는다.**
- 키가 없으면 연결 미완료 상태를 표시하며 실제 플레이가 완성됐다고 보고하지 않는다.
- 연결 후 한국어 지침으로 실제 호출을 검증한다.

Jev는 문장의 행동과 적용 상황만 해석한다. 생존·점수·삭제 비용은 게임 코드가 결정한다.

**`POST /api/interpret`**

- 입력: 새 지침, 던전 버전, 해석 규칙 버전.
- 출력: 행동 종류, 정해진 관찰 상황별 적용 여부, 불확실성, 사용 모델 버전.
- 오류: 입력 문제, 지원 외 지시, 해석 불확실, 요청 제한, 공급자 연결 실패를 구분한다.

서버가 보유한 상황 데이터를 사용하며 클라이언트가 임의의 프롬프트나 대량 상황을 전달하게 하지 않는다. 정답표와 생존 결과는 AI에 주지 않는다.

- 새 지침 확정 전에 필요한 상황의 해석을 준비한다. 게임 진행 중 장애물마다 네트워크 응답을 기다리지 않는다.
- 사용자에게는 현재 또는 이미 경험한 상황의 해석만 보여준다.
- 확정된 해석은 저장해서 같은 도전에서 재사용한다.
- 사망 설명은 실제 실행 기록으로 만든다.
- 불확실한 해석은 확정하지 않는다. 판단 기준은 한국어 평가 사례로 검증해 해석 버전에 포함한다.
- 요청 제한 시간은 15초로 두고 오류 시 입력과 게임 상태를 보존한다.
- 통신 오류로 데스·아이템·작성 기회를 소비하지 않는다.
- 중복 요청과 수정 전 문장의 늦은 응답을 무시한다.

### 저장과 공유

브라우저에 지침·확정 해석·점수·지우개·진행 위치·튜토리얼 완료 여부·개인 최고 기록을 저장한다.

- 주요 상태 변경 직후 저장한다.
- 새로고침하면 마지막으로 확정된 판단 지점에서 재개하고 비용을 중복 반영하지 않는다.
- 게임·던전·해석 버전을 저장한다. 호환되지 않는 기록을 조용히 재해석하거나 삭제하지 않는다.
- 저장 실패와 손상을 안내하고 기존 데이터를 보존한다.
- 다른 탭에서 같은 기록이 변경되면 현재 탭을 멈춰 덮어쓰기를 방지한다.
- 기기 간 동기화와 서버 기록은 제공하지 않는다.

클리어 시 게임 링크·데스 내역·선택적으로 최종 지침을 공유한다. 실제 공유 내용을 먼저 보여주고 시스템 공유 또는 복사를 제공한다. 친구는 같은 던전을 새 기록으로 시작한다.

### 배포

- Vercel에 프런트엔드와 서버 API를 함께 배포한다.
- 키는 서버 비밀 환경변수로만 관리하고 프런트엔드 번들에 포함하지 않는다.
- 공개 링크는 로그인 없이 접속하게 하며 초기에는 검색 노출을 막는다.
- AI API에 IP별 분당 30회 요청 제한을 초기값으로 적용한다. 입력 길이와 서버 작업량도 제한한다.
- 요청 제한을 전체 비용 상한으로 취급하지 않는다. 호출 수·토큰·지연·오류를 확인하고 AI 호출 중단 설정을 둔다.
- 운영 로그에는 지침 원문과 비밀값을 남기지 않는다.
- 배포 계정 연결이 필요하면 요청하며 유료 플랜을 임의 구매하지 않는다.

## 5. 제작 순서와 완료 기준

### 1단계 — 시각 기준과 기본 구조

- React·Vite·Canvas·게임 코어·API 구조를 준비한다.
- Jev 키 발급·설정을 사용자에게 요청한다.
- 대표 방, 캐릭터, 메모장, 입력창을 함께 구성한다.
- 필요하면 ImageGen으로 시안을 만들고 최종 자산의 기준을 정한다.
- 데스크톱과 모바일에서 실제 게임 화면으로 확인한다.

**완료 기준:** 임시 사각형이나 기본 웹 폼 수준을 넘어, 최종 게임의 분위기와 조작 구조가 드러난다.

### 2단계 — 대표 구간을 높은 품질로 완성

다음 경험 하나를 끝까지 연결한다.

**입력 → 해석 확인 → 이동 → 사망 → 새 지침 → 부활 → 성공**

- 실제 Jev 호출을 사용한다.
- 캐릭터 모션, 화면 전환, 버튼 반응, 효과음, 기다림을 다듬는다.
- AI 연결이 미완료이면 해당 구간을 완료로 판단하지 않는다.
- 이 구간이 거칠면 방을 늘리기 전에 개선한다.

**완료 기준:** 처음 보는 사람이 조작을 이해하고, 자신이 쓴 문장으로 행동이 바뀌었다는 것을 알아볼 수 있다.

### 3단계 — 규칙과 던전 확장

- 삭제·지우개·패널티·저장·막힘 복구를 구현한다.
- 검증한 시각·모션 기준으로 본편 방을 확장한다.
- 빠른 재생, 마지막 연속 통과, 클리어 보상을 완성한다.
- 추가 콘텐츠 때문에 품질이 낮아지면 중복 학습 방부터 줄인다. 학습→재사용→예외→조합 구조는 유지한다.

**완료 기준:** 처음부터 클리어까지 플레이할 수 있고, 삭제로 과거 구간의 결과가 바뀌는 경우도 납득 가능하다.

### 4단계 — 실제 화면과 동작 검수

| 검수 영역 | 완료 조건 |
|---|---|
| 시각 | 에셋·색감·픽셀 밀도·폰트가 일관되고 임시 자산이 남지 않음 |
| 움직임 | 걷기·도약·착지·사망·부활 사이에 눈에 띄는 끊김이나 위치 튐이 없음 |
| 입력 | 즉각적인 반응, 한국어 조합 입력, 중복 클릭 방지 |
| 기다림 | AI 대기 중 화면과 입력 보존, 현재 상태와 복구 방법이 명확함 |
| 반복 | 이미 본 구간은 빠르게, 달라진 행동은 알아볼 수 있게 재생 |
| 모바일 | 키보드·좁은 화면·텍스트 확대에서 핵심 조작이 가려지지 않음 |
| 규칙 | 최신 적용 지침, 삭제 비용, 패널티 작성 기회 금지, 점수 내역이 정확함 |
| 저장·오류 | 새로고침·통신 실패·저장 오류에서 비용과 기록이 잘못 바뀌지 않음 |

- 스크린샷으로 보는 완성도와 실제 플레이의 완성도를 각각 확인한다.
- 타입 검사·빌드·핵심 게임 규칙 테스트를 실행한다.
- 한국어 문장·상황 쌍 최소 60개로 실제 Jev 해석을 평가한다. 정상 표현·예외·부정·모호한 입력·지원 외 지시를 포함한다.
- 초기 목표는 평가 세트 기대 판정 일치율 95% 이상이며 튜토리얼·대표 클리어 사례는 모두 통과해야 한다. 이 수치를 전체 한국어 정확도로 표현하지 않는다.

### 5단계 — 공개 배포와 전달

- 로그인하지 않은 브라우저에서 접속부터 실제 AI 클리어까지 확인한다.
- 데스크톱과 모바일에서 공유 링크·저장·재도전을 확인한다.
- 실제 플레이 중 API 키가 노출되지 않고 요청 제한이 적용되는지 확인한다.
- 소스, 공개 URL, 실행·환경 설정 안내, 검증 결과와 제한 사항을 전달한다.

**최종 완료는 “접속되고 기능이 작동함”을 넘어, 처음 시작해서 다시 도전하고 클리어하는 과정이 시각적으로 완성돼 있고 매끄러운 상태다.** 사람 대상 사용성 테스트 결과는 직접 관찰한 뒤 별도로 기록한다.
