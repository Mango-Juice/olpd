# 개발 현황과 후속 계획

기준: 2026-09-22 로컬 소스. 최초 제작 계획은 Git 기준 커밋 `550c3bb`에 보존한다. 이 문서는 현재 구현과 남은 작업을 구분하며, 실행 방법은 [README](README.md), 검증 근거는 [검증 기록](docs/verification.md)을 따른다.

## 목표와 현재 범위

자연어 지침을 한 줄씩 남기며 작은 용사를 던전 밖으로 이끄는 웹 게임이다. 화면의 가독성, 부드러운 이동, 실패 이유를 이해하고 재도전할 수 있는 경험을 우선한다.

- React + TypeScript + Vite, Canvas 2D, Node.js API, 실제 TypeSafe Jev.
- 튜토리얼과 본편 8개 방. 행동은 전진·점프·숙이기·우회 네 종류다.
- 현재 8개 방 전체가 `memory-dungeon` 한 스테이지다.
- 브라우저 자동 저장, 클리어 결과 공유, 생별 모험 연혁과 별도 보관함을 제공한다.
- 모험 연혁과 이번 정리 변경은 로컬 소스 기준이다. 공개 배포 검증은 [검증 기록](docs/verification.md)에 날짜별로 남긴다.

## 현재 플레이 규칙

| 항목 | 구현된 동작 |
| --- | --- |
| 지침 | 앞뒤 공백을 제외한 Unicode 코드 포인트 1~80자, 한 종류의 행동 |
| 해석 | 첫 학습은 별도 확인, 이후 유효한 입력은 기억·출발로 이어짐 |
| 우선순위 | 화면 위쪽의 적용 가능한 메모 한 줄, 해당 메모가 없으면 전진 |
| 순서 변경 | 출발 전 손잡이 드래그 또는 `···` 메뉴의 화살표, 무료·자동 저장 |
| 충돌 | 적용 상황 전체 집합이 같고 행동만 다르면 새 메모 저장 거부; 부분 중첩·같은 행동 허용 |
| 작성 기회 | 본편 사망당 최대 한 줄, 출발 때 소멸·누적되지 않음 |
| 삭제 | 사망 후 가능. 지우개 2개를 먼저 사용하고 소진 후 한 줄당 +3데스 |
| 점수 | 사망·자진 부활 + 삭제 패널티. 삭제 패널티는 작성 기회를 주지 않음 |
| 재도전 | 던전 입구에서 다시 출발하며 지침·지우개·기록 유지 |
| 튜토리얼 | 작성한 지침은 본편으로 이월, 데스는 0·지우개는 2개로 시작 |
| 재생 | 신규 행동 4.2초·반복 1.4초, 일시정지·한 장면씩 보기·탭 숨김 자동 정지 |

지침 본문은 확정 후 직접 수정하지 않고 삭제·추가한다. 생사·점수·비용은 순수 게임 코어가 먼저 확정하고 저장하며, Canvas는 결과만 재생한다. 프레임률은 판정에 영향을 주지 않는다.

## 저장과 기록의 경계

- 진행 중 기록은 localStorage, 완료한 모험은 IndexedDB에 여정 ID별로 한 번 보관한다.
- 연혁은 메모 작성·삭제·순서 변경·부활·행동 결과를 기록한다. 장면 다시 보기는 게임 진행과 비용을 변경하지 않는다.
- 구버전 저장의 행동 이력은 복원하되, 당시 남기지 않은 메모 변경 이력은 만들어 내지 않는다. 저장 호환 코드는 유지한다.
- 손상·비호환 데이터는 자동 삭제하지 않는다. 다른 탭이 기록을 변경하면 현재 탭을 멈춘다.
- 계정·서버 동기화·기기 간 저장 이전은 구현 범위에 포함하지 않는다.

## AI와 운영 계약

- Jev는 행동과 적용 상황을 해석하고, 게임 코드가 결과·점수·비용을 결정한다.
- 키는 서버의 `TYPESAFE_API_KEY`로만 관리한다. 실제 플레이에 모킹·키워드 대체 해석을 넣지 않는다.
- 실패 시 입력과 작성 기회를 보존한다. 요청 제한 시간은 15초, 본문은 2 KiB, IP별 제한은 서버 인스턴스당 분당 30회다.
- `AI_ENABLED=false`로 공급자 호출을 중단한다. 호출 원문·키는 운영 로그에 남기지 않는다.
- Vercel의 API·WAF·배포 결과는 별도 운영 상태다. 로컬 빌드 성공을 배포 성공으로 취급하지 않는다.
- 세부 요청/오류 계약은 [API 문서](docs/api.md)를 따른다.

## 후속 작업

1. [10장 스테이지 설계](docs/stage-design/README.md)와 [스토리 구상](docs/story-concept.md)은 제안 상태다. 자유로운 대상·도구·순차 행동, 장별 퍼즐과 보스는 아직 구현하지 않았다.
2. 캠페인 확장 시 현재 네 행동 해석과 저장 구조의 재사용 범위를 먼저 결정한다. 규칙 변경은 버전과 명시적인 마이그레이션으로 다룬다.
3. 실제 iOS/Android 기기에서 키보드와 터치, 첫 플레이 이해도, 소요 시간을 관찰한다. Chromium 에뮬레이션과 자동화 결과로 이를 대신 주장하지 않는다.
4. 모험 연혁과 후속 변경의 배포는 별도 작업으로 진행한다.

## 변경 시 확인할 것

- `npm test`, `npm run build`로 순수 판정·저장·API 계약과 타입을 확인한다.
- UI·재생·저장 흐름을 바꾸면 관련 브라우저 검사를 실행하고 화면을 확인한다.
- AI 프롬프트·모델·판정 임계값을 바꾸면 실제 Jev 평가를 수행한다. 고정 평가 세트의 결과를 전체 한국어 정확도로 일반화하지 않는다.
- 검증 날짜, 명령, 성공/실패와 관찰 한계를 [검증 기록](docs/verification.md)에 남긴다.
